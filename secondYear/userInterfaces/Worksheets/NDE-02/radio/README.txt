Compare this with the 'checkbox' demo.

Note how the radio buttons are mutually exclusive, as you'd expect.
Qt is able to enforce this behaviour because the three buttons
have all been added to the same container.
